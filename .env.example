# Syntaxis Skill Check — variáveis de ambiente
# Copie este arquivo para .env.local e preencha os valores reais.
# NUNCA commitar .env.local (já está no .gitignore).

# MailerLite — usada no Épico 7 (captura de lead), somente em código server-side
# (Route Handlers). Nunca deve ser exposta ao client nem prefixada com NEXT_PUBLIC_.
# Gerada em: painel MailerLite → Integrations → MailerLite API.
MAILERLITE_API_KEY=

# IDs dos Grupos MailerLite correspondentes a cada classificação do resultado.
# Criar os grupos "Baixo"/"Médio"/"Alto" no painel primeiro (ver README.md,
# seção "Setup do MailerLite") e colar aqui o ID numérico de cada um.
MAILERLITE_GROUP_ID_BAIXO=
MAILERLITE_GROUP_ID_MEDIO=
MAILERLITE_GROUP_ID_ALTO=
