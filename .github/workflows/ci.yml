name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint-typecheck-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Typecheck
        run: npm run typecheck

      - name: Validate questions bank
        run: npm run validate:questions

      - name: Lint hardcoded colors
        run: npm run lint:colors

      - name: Audit token contrast
        run: npm run audit:contrast

      - name: Lint one-pattern-per-file
        run: npm run lint:patterns

      - name: Verify asset manifest
        run: npm run assets:verify-manifest

      - name: Verify asset palette
        run: npm run assets:verify-palette

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run e2e (design system, landing page)
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers (Chromium, usado pelo Lighthouse)
        run: npx playwright install --with-deps chromium

      - name: Build
        run: npm run build

      - name: Lighthouse — Performance/Acessibilidade/SEO ≥ 90 (/)
        run: npm run test:lighthouse

      - name: Lighthouse — 4 rotas × claro/escuro ≥ 90 (Épico 19)
        run: npm run test:lighthouse:flow
